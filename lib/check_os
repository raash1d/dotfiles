check_os() {
    OS=''
    case "$(uname)" in
        [dD]arwin)
            OS='mac'
            ;;
        [lL]inux)
            OS='linux'
            ;;
        *)
            echo 'OS not supported'
            exit -1
            ;;
    esac
    echo ${OS}
}

check_os
